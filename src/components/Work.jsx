const Work = (props)=> {
    //const project = props.projects;
    return (

    
{
    <div> projects= {projects} <div/>
    
    /* <div>
      <div>
        {props.projects.map((work) => (
          <div className="card">
            <h1>Project: {project.title}</h1>
                    <img src={process.env.PUBLIC_URL + project.image} alt="Placeholder image" />
              </div>
              ))}
      </div>
    </div> */}

    )}

export default Work